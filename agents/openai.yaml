interface:
  display_name: "UI Prototyper"
  short_description: "Design UI prototypes with Mobbin and imagegen"
  default_prompt: "Use $ui-prototyper to select visually current Mobbin references, directly generate three reference-faithful main-screen design languages, and continue after I choose one."
dependencies:
  tools:
    - type: "mcp"
      value: "mobbin"
      description: "Mobbin MCP for searching real screens, flows, and web sections before UI image generation."
