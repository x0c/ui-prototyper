interface:
  display_name: "UI Prototyper"
  short_description: "Design UI prototypes with Mobbin and imagegen"
  default_prompt: "Use $ui-prototyper to turn one real product moment into three bold, original visual worlds—hero first, controls second, no reference-image attachment in exploration."
dependencies:
  tools:
    - type: "mcp"
      value: "mobbin"
      description: "Mobbin MCP for searching real screens, flows, and web sections before UI image generation."
