interface:
  display_name: "UI Prototyper"
  short_description: "Design UI prototypes with Mobbin and imagegen"
  default_prompt: "Use $ui-prototyper to lock the product requirements, research scoped Mobbin design principles, and directly generate three original product-first main-screen directions."
dependencies:
  tools:
    - type: "mcp"
      value: "mobbin"
      description: "Mobbin MCP for searching real screens, flows, and web sections before UI image generation."
